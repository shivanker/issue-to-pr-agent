# GitHub App credentials
GITHUB_APP_ID=your-app-id
GITHUB_PRIVATE_KEY=your-private-key
GITHUB_WEBHOOK_SECRET=your-webhook-secret
GITHUB_CLIENT_SECRET=your-client-secret-if-needed

# Installation ID (optional - usually extracted from webhook payload)
# Only needed for testing outside of webhook context
GITHUB_INSTALLATION_ID=your-installation-id

# GitHub repository for local testing (used if not specified in webhook payload)
GITHUB_OWNER=your-github-username
GITHUB_REPO=your-repo-name

# Git configuration
GIT_WORK_DIR=/tmp/issue-to-pr-agent
GIT_AUTHOR_NAME=GitHub Issue Bot
GIT_AUTHOR_EMAIL=bot@example.com

# AWS settings (for deployment)
AWS_REGION=us-east-1

# Server settings
PORT=3000